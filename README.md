The coords file is a generated hilbert curve, used https://github.com/galtay/hilbert_curve to generate.

Sample.json is indexed data merged into hilbert curve data for rendering purposes. Script to generate sample.json to be released
but will need to be integrated into the server.

To start the app, from the `sample_site` directory run:
```python -m SimpleHTTPServer```